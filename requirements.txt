# --- Core Web Framework ---
Flask==3.0.3

# --- Optional: If you want automatic reloads during development ---
Werkzeug==3.0.2

# --- JSON and CLI integration helpers (standard but safe) ---
click==8.1.7
itsdangerous==2.2.0
Jinja2==3.1.4

# --- Only needed if you plan to parse JSON reports deeply (optional) ---
markupsafe==2.1.5
